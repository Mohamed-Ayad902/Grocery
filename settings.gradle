pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        jcenter() // for image slider lib
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        jcenter() // for image slider lib
        mavenCentral()
    }
}
rootProject.name = "Grocery"
include ':app'
